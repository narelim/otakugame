# 서코의 신 — 정식 게임 개발 로드맵

> 결정된 방향: **웹 기반 유지**(React + Vite). Unity 재작성 안 함 — 핵심인 그림 앱(Canvas 2D)을 살리기 위함.
> 최종 목표: **Steam(PC) + 모바일(iOS/Android)**. 온라인 교류회(2D 도트 아바타, 게더타운형) + AI API 연동.

---

## 아키텍처 (목표 형태)

```
[클라이언트]  React (UI · 그림앱 · SNS · 굿즈)          ← 현재 코드 대부분 유지
        +  PixiJS 레이어 (교류회 플로어: 2D 도트 아바타)  ← 신규
        │
        │  Electron 으로 감싸 → Steam
        │  Capacitor 로 감싸 → iOS/Android
        ▼
[백엔드]  Node + Colyseus (실시간 룸: 교류회)
        +  AI 프록시 (Claude API 호출 · 레이트리밋 · 캐싱)
        +  DB (계정 · 세이브 · 굿즈/부스 데이터)
```

기술 선정 근거:
- **PixiJS**: 게더타운도 쓰는 2D WebGL 렌더러. 도트 아바타 공유 공간에 적합.
- **Colyseus**: Node 기반 authoritative 멀티플레이 룸 서버. 교류회 방(room) 모델에 직결.
- **Electron / Capacitor**: 웹 코드 하나로 Steam + 모바일 커버. 그림 앱 재작성 0.
- **AI 프록시 필수**: API 키 노출·CORS 때문에 클라 직접 호출 불가. 멀티용 백엔드가 겸함.

---

## Phase 0 — 기반공사 ✅ (완료)

정식 게임의 토대. 멀티/AI 붙이기 전에 반드시 튼튼하게.

- [x] **세이브/로드** — IndexedDB 자동저장(디바운스 800ms) + 타이틀 "이어하기" (`systems/saveSystem.js`)
- [x] **코드 구조 정리** — App.jsx 1,830줄 → systems/screens/components 분리 (App.jsx는 60줄 라우팅 셸)
- [x] **가로 풀화면 레이아웃** — 기본 진입 = 가로 데스크톱 셸(1920×1080 스케일 스테이지). 세로 모바일은 `#mobile` 레거시 라우트로 유지(같은 state 공유). maitalk 앱은 준비중 플레이스홀더.
- [x] **에러/안정성** — 공용 `ErrorBoundary`(App·데스크톱 앱창·폰·현생 모드에 적용), 세이브 버전 필드 + `migrate()` 훅 + `normalizeLoaded()` 누락 필드 보정

## Phase 1 — 게임성 완성 (싱글플레이)

- [x] 부스 꾸미기 개편 (부.꾸): 실측(cm) 기반 드래그 배치 에디터 — 존 4개(상단배너/뒷벽/테이블위/테이블앞), 종류별 최대 개수, 효과 개수 합산, 테이블보 기성품·현수막 내그림 인쇄, 전시대→그물망랙·계단식진열대 재해석 (`data/boothData.js` + `BoothPlannerApp.jsx`)
- [x] **알바 요일제 + 미니게임** — 알바별 근무 요일(day%7), 알바냥 앱에서 출근 → 타이밍 바 미니게임(3라운드, 알바별 스킨) → 성과 배율(×0.5~1.4) 일당 적립 → 월급날 합산 입금. 출근 안 하면 일당 없음, 행사 당일 휴무, 캘린더에 근무일 표시. 알바별 전용 게임(바코드 리듬 등)은 추후 확장
- [x] **행사 당일 연출** — `EventDayScreen`(데스크톱): 부.꾸 레이아웃 실렌더(BoothScene) + 손님 스프라이트·구매 말풍선 + 오픈→피크→마감 페이즈 + 실시간 재고/골드 티커. 계산은 `systems/eventSim.js`로 분리(레거시 EventScreen과 공유)
- [x] **현생 1인칭 리뉴얼** — `MyRoomScreen`: 내 방 정면 뷰, 가구 클릭으로 행동(침대=휴식/취침·냉장고=밥·책장=원작수혈·굿즈선반=굿즈구경·폰=숏츠/뉴짤/폰열기·현관=외출/행사장·컴퓨터=데스크톱 복귀). 시간대별 창밖/조명(오전→오후→저녁), 벽에 내 그림 포스터·선반에 내 굿즈 목업, 폰(PhoneOS) 오버레이. 로직은 `systems/dailySystem.js`로 분리(레거시 공유)
- [ ] **아바타 + 코스튬 쇼핑몰** — 인터넷에 쇼핑몰 사이트 추가. 파츠 규격은 Phase 3 교류회 도트 아바타와 공용으로 설계 (재작업 방지). 노출: 폰 프로필·현생·부스 지킴이
- [ ] **공식 굿즈 수집 (덕질장)** — "공식 굿즈 구경" 액션 시 수집품 획득. 장르 완전 맞춤 불가 → 캐릭터 외형 태그에서 팔레트 + 분위기 태그에서 모티프를 뽑아 절차적 생성(goodsMockup 재활용). 폰 컬렉션 북 + 세트 완성 소프트 보너스
- [ ] **장르 엔딩 = 계정 정리(졸업)** — 아래 설계 노트 참조
- [ ] 콘텐츠 볼륨 확장 + 밸런싱 (골드/체력/멘탈/인기도 곡선)
- [ ] 튜토리얼 / 온보딩
- [ ] 사운드 · 폴리싱 · 연출 고도화

### 설계 노트: 엔딩 (2026-07 결정)
- 게임 전체 엔딩은 없음(샌드박스). 엔딩은 **장르 단위의 "계정 정리"**이고 결정 주체는 플레이어.
- 정리 방식 2종: **휴덕**(계정 유지, 복귀 시 수치 일부 감쇠) / **탈덕**(계정 close + 사유 선택).
- close된 장르는 삭제가 아니라 **기록 보관소(회고록)**로: 활동 일수·총 판매·대표 굿즈·명장면 로그 영구 보존.
- 다른 장르 활동 중 옛 장르 소식이 간간히 SNS에 흘러옴 (트윗 템플릿에 archived 장르 참조 카테고리).
- **재파기(복덕)**: 탈덕한 장르를 다시 파면 "회귀 이벤트" — 새 계정으로 시작하되 옛 기록이 링크되어
  초기 팔로워 보너스(예전 팬이 알아봄) + 낮은 확률 "흑역사 발굴" 이벤트. archive 데이터는 불변, 활동 수치는 새로.
- 전체 진행 마일스톤은 "서코의 신 전당" 칭호로 (Steam 도전과제 후보).

### 콘텐츠 백로그: 바쁜 오타쿠 (2026-07)
설계 축: 유한한 시간(행동 슬롯)·골드·체력·멘탈 위에서 콘텐츠들이 **서로 경쟁** →
선택이 플레이어의 오타쿠 성향을 드러냄. 성향 통계(시간/돈 사용처)를 추적해
회고록 문장("가챠에 ₩84만 사용")과 성향 칭호("가챠의 노예"·"리셀 헌터"·"무과금 성좌")로 환원.

- [ ] **메루카리(중고장터)** — 인터넷 사이트. 구매: 절판 공식 굿즈 프리미엄 시세(덕질장 구멍 메우기)·상태등급·가품 리스크. 판매: 행사 이월 재고 판로, 가격 후려치기·사기 이벤트(메시지 연동), 리셀 논란(팬신뢰 vs 골드)
- [ ] **티켓팅** — 정해진 날짜·정각 선착순 미니게임. 성공 시 캘린더에 덕질 일정(콘서트·팝업·생카) 등록 → 행사 마감과 일정 충돌 압박의 공급원. 실패 → 메루카리 양도 티켓으로 연결
- [ ] **응모 당첨** — SNS/일상에서 원클릭 응모, 며칠 뒤 메시지로 결과 도착. 낮은 확률 × 큰 보상(멘탈 회복 + 수집품)
- [ ] **온라인 가챠** — 폰 앱(장르 IP 모바일게임 컨셉). 천장·픽뚫·10연 연출, 골드·멘탈 소모, 결과는 덕질장 수집 + mabo 자랑 포스트 연동
- [x] **성향 통계 추적 (기반)** — `logTx`에 카테고리 인자 추가, `state.stats.{spend,earn}`에 누적(세이브 v4). 현재 카테고리: goods/event/booth/daily/job — 회고록·칭호는 추후 이 데이터로

## Phase 2 — AI 연동

- [ ] 백엔드 프록시 서버 (Node) — Claude API 안전 호출
- [ ] `generateImage` → 실제 이미지 생성 (팬아트/굿즈)
- [ ] `buildNpcRoster` / `pickTweet` → LLM 기반 NPC·트윗 생성
- [ ] 비용 통제: 레이트리밋 · 캐싱 · imageTicket 소비 설계

## Phase 3 — 멀티플레이 교류회 (메타버스)

- [ ] Colyseus 룸 서버 (교류회 = room)
- [ ] PixiJS 2D 도트 아바타 플로어 (이동/충돌/부스 배치)
- [ ] 계정/인증 · 남의 부스 방문·구경 · 실시간 채팅
- [ ] 굿즈/부스 데이터 서버 동기화

## Phase 4 — 출시 패키징

- [ ] Electron 래핑 + Steamworks (도전과제/클라우드세이브) → Steam
- [ ] Capacitor 래핑 → iOS/Android 스토어
- [ ] 빌드/배포 파이프라인, 스토어 심사 대응

---

## 개발 관행 (업데이트)
- 수정 후 `npm run build`로 검증. 베이스라인: 빌드 성공(~540KB, 2026-07 기준).
- state에 `saveVersion` 필드 두고, 로드 시 구버전 마이그레이션.
- 큰 UI 변경 전 코드 분리 우선 → 유지보수/멀티 확장 대비.
